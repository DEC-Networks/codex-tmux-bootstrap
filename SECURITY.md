# Security v26.7.14.1

Report vulnerabilities through GitHub's private vulnerability reporting for
this repository. Do not include credentials or sensitive host data in a public
issue.

The bootstrap verifies the full runner with SHA-256 before execution. Release
assets include `SHA256SUMS`. Users who require immutable inputs should use a
tagged release instead of the moving `main` branch.
